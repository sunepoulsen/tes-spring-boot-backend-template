# ===================================================================
#                   Spring configuration
# ===================================================================

spring:
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true

  datasource:
    url: 'jdbc:h2:mem:db;DB_CLOSE_DELAY=-1'
    username: 'sa'
    password: 'sa'
    driver-class-name: 'org.h2.Driver'
